import styled from "styled-components";

const Container = styled.div`
  padding: 0.8rem;
  color: #eee;
  text-align: center;
  background-color: #1b9096;
`;

const Header = () => {
  return <Container>일정관리</Container>;
};

export default Header;
